# Var & Const 


```rust
let x = 5; // immutable, type inferred
let x = 10; // shadowing (redeclare same name)
let mut y = 10; // mutable
const MAX: u32 = 100;   // constant, must have type
static PI: f64 = 3.14;  // static, lives entire program
```
